DATABASE_URL="file:./dev.db"
PORT=4000
JWT_SECRET="replace-with-a-long-random-secret"
# Comma-separated list of browser origins allowed to call the API. e.g.
# CORS_ORIGIN="http://localhost:3000,https://gg-sports.vercel.app,https://ggsports.in"
CORS_ORIGIN="http://localhost:3000"
# Seed credentials (used by `npm run seed`). Change before any public deploy.
ADMIN_EMAIL="admin@ggsports.in"
ADMIN_PASSWORD="ChangeMe123!"
